# 调试证据与 Markdown 标题树变更记录

## 范围

本次将一段已脱敏的用户管理接口排障对话提炼为通用规则，并加入统一项目工作流。未写入历史对话中的地址、账号、密码、令牌、Cookie、长 ID 或完整响应。

## 变更

- `bugfix`（原 debug）增加最小请求卡、写后读验证对和失败回交规则。
- `dev` 增加接口验收边界；`memory-put` 只归档脱敏证据。
- 维护 `tools/http-check.sh`，默认只输出状态码和断言结果。
- 新增 `project-memory.js outline --file <项目内 Markdown> --depth <1-6>`，按需返回标题树。
- 明确 `get --tree`、遗留 `gotcha-index` 和 `outline` 的职责边界。

## 验证边界

初次实施时只做源码和文档静态审阅，未运行 Node、Bash、真实登录或接口请求。后续隔离验收已覆盖路径安全、代码围栏、标题跳级、深度截断、传输失败、状态码失败和秘密头缺失；见 [运行时验收记录](../project-memory/2026-08-17_运行时验收与路由收敛.md)。
