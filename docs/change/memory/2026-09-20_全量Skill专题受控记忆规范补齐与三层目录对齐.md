# 2026-09-20 全量 Skill 专题受控记忆规范补齐与三层目录对齐

- 摘要: 补齐 docs/change/ 全量 8 个独立专题子目录与 1:1 强绑定，迁移 debug 变更至 bugfix 并对齐共享会话规范，规范化全量 11 个 docs/memory/*.md 专题受控记忆二层结构，更新 docs/MEMORY.md 索引，通过完整性校验。
- 价值评估: high - 完善三层记忆拓扑架构与专题 1:1 强绑定落地
- 有效性: active
- 关联API: [project-memory-cli](../../api/project-memory-cli.md)
- 关联专题: [memory](../../memory/memory.md), [bugfix](../../memory/bugfix.md), [dev](../../memory/dev.md), [workflow](../../memory/workflow.md)

---

#### 一、问题/需求

落实治理中枢派发的《全量 Skill 专题受控记忆规范补齐与三层目录对齐》工单：
1. 补齐 `docs/change/` 下所有专题的独立子目录，落实三层受控记忆 1:1 强绑定；
2. 响应用户指令将 debug 命名收敛为 bugfix，将既有 `docs/change/debug/` 变更记录规范迁移至 `docs/change/bugfix/`，并与 dev 专题对齐共享物理会话规范；
3. 规范化全量 `docs/memory/*.md` 专题记忆二层结构（架构职责、真实白名单、关键事实与变更索引标记）；
4. 在 `docs/MEMORY.md` 完整登记 11 个专题与最新变更路径，通过 `checkIntegrity` 零断链校验。

---

#### 二、原因分析

1. 历史专题在初始拉起时仅创建了文档骨架，缺少真实的物理白名单范围声明与变更索引标签；
2. 用户对工作流命名进行了调整（debug 改回 bugfix，且 bugfix 与 dev 共享同一常驻物理会话）；
3. 原 `docs/change/` 仅有 `debug`、`memory` 和 `project-memory`，其余各 Skill 缺乏 1:1 绑定的归档落地目录。

---

#### 三、测试结果

1. **目录补齐与迁移**：
   - 补齐了 `docs/change/{bugfix, dev, workflow, explore, memory-get, memory-put, session-reader, session-gotcha-extractor}/` 独立子目录；
   - 既有调试变更已完整迁移至 `docs/change/bugfix/2026-08-17_调试证据与Markdown标题树.md`，旧路径安全清理；
2. **完整性校验**：
   - 执行 `node skills/project-memory/scripts/project-memory.js inspect --project-root .`：
     * `framework.status: ready`；
     * `integrity.status: valid`，已核验受控文档 13 篇，专题 11 个，全局变更 12 篇，API 契约 1 篇，断链与警告均为 0，Exit Code: 0；
3. **检索验证**：
   - 执行 `node skills/project-memory/scripts/project-memory.js get --query "bugfix" --project-root .`，准确跨三层检索到主记忆、专题记忆、变更明细与 dev 关联事实。

---

#### 四、备注

1. **会话共享规范**：bugfix 与 dev 共享实体物理会话（ID: `c1e90711-1db3-47a9-9253-d765bc80fcb8`），后续相关排障与开发变更分别归档入各自独立变更子目录；
2. **持续守约**：后续任何新增变更必须严格遵循四段式结构与脱敏红线要求。
